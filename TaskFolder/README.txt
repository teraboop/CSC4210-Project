Task2 takes in a text file with a truth table, text file must be within same directory as the task file.
Input the number of variables, the text file name (no extension) and the desired output format.

Task3 takes in a text file with numerous 32 bit instruction that is provided with my submission, you may need to change pathing for the text file open request.